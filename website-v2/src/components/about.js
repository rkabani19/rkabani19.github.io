import React from 'react';
import styled from 'styled-components';

const AboutContainer = styled.div`
  background-color: #fed837;
`;

export default () => (
  <AboutContainer>
    <p>React stuff dude</p>
  </AboutContainer>
)